Git 
vcs - version control system

Requirements
History of changes
backup of the code
team collaboration

vcs - tool to track, manager, control the source code

github vs git
git is a tool but github is a website

git was created by the creator of linux

bitbucket, git 

git init to initialize the git (empty repo)
git status  to know whether git is connected
git add filename to add -- staging area
git add . add all from dir
git rm --cached filename to remove the filename added (before commiting)
git commit -m "message of commit" -local commit (snapshot the code which you changed)
git push -- code moves to remote repo
git log to check the log
git help to find diff commands


